{
  "name": "cloud-sync",
  "version": "1.0.0",
  "description": "Cloudflare Worker backing state sync for all four sister apps",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
